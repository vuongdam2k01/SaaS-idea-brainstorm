# Sensitive raw material per idea (transcripts, real names, payments)
ideas/*/private/*
!ideas/*/private/.gitkeep

# Internal dogfood workspaces live OUTSIDE this repo entirely (archived at
# ../SaaS-idea-brainstorm-dogfood-archive). If one is ever recreated here, keep
# it out of ideas/ (the session-start sentinel would report it as a user idea)
# and out of git:
dogfood/

node_modules/
.DS_Store

# eval artefacts: fixtures are generated, results are per-run
evals/fixtures/generated/
evals/results/
